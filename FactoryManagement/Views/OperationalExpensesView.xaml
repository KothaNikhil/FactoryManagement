<UserControl x:Class="FactoryManagement.Views.OperationalExpensesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:FactoryManagement.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <Border Grid.Row="0" Background="{StaticResource Card.Header}" Padding="20" Margin="0,0,0,20" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Background="{StaticResource Card.Warning}" Style="{StaticResource IconBadge}" Width="48" Height="48" CornerRadius="24">
                    <TextBlock Text="💰" FontSize="28" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock Text="Operational Expenses" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Track fuel, repairs, utilities, and other operational costs" FontSize="14" Foreground="{StaticResource Text.Secondary}" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Summary Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Expenses -->
            <Border Grid.Column="0" Background="{StaticResource SummaryCardDanger}" CornerRadius="8" Padding="16">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.3" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="💸" FontSize="32" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Total Expenses" Foreground="{StaticResource LabelDanger}" FontSize="11" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding TotalExpenses, StringFormat='₹{0:N2}'}" 
                                   Foreground="White" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Monthly Expenses -->
            <Border Grid.Column="2" Background="{StaticResource SummaryCardWarning}" CornerRadius="8" Padding="16">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.3" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="📅" FontSize="32" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="This Month" Foreground="{StaticResource LabelWarning}" FontSize="11" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding MonthlyExpenses, StringFormat='₹{0:N2}'}" 
                                   Foreground="White" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Today's Expenses -->
            <Border Grid.Column="4" Background="{StaticResource SummaryCardInfo}" CornerRadius="8" Padding="16">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.3" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🕒" FontSize="32" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Today" Foreground="{StaticResource LabelInfo}" FontSize="11" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding TodayExpenses, StringFormat='₹{0:N2}'}" 
                                   Foreground="White" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Expense Count -->
            <Border Grid.Column="6" Background="{StaticResource SummaryCardSuccess}" CornerRadius="8" Padding="16">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.3" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="📊" FontSize="32" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Records" Foreground="{StaticResource LabelSuccess}" FontSize="11" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding ExpenseCount}" 
                                   Foreground="White" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: Add Expense Form -->
            <Border Grid.Column="0" BorderBrush="#444" BorderThickness="1" CornerRadius="8" Background="#2a2a2a" Padding="15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Form Title -->
                        <TextBlock FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Add New Expense"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                            <Setter Property="Text" Value="Edit Expense"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Category -->
                        <TextBlock Text="Category *" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding ActiveCategories}" 
                                  SelectedItem="{Binding SelectedCategory}"
                                  DisplayMemberPath="CategoryName"
                                  Style="{StaticResource MaterialDesignComboBox}" Margin="0,0,0,15"/>

                        <!-- Amount -->
                        <TextBlock Text="Amount *" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource MaterialDesignTextBox}" Margin="0,0,0,15"/>

                        <!-- Expense Date -->
                        <TextBlock Text="Expense Date *" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,0,0,5"/>
                        <DatePicker SelectedDate="{Binding ExpenseDate}" 
                                    Style="{StaticResource MaterialDesignDatePicker}" Margin="0,0,0,15"/>

                        <!-- Vendor Name -->
                        <TextBlock Text="Vendor Name" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding VendorName, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource MaterialDesignTextBox}" Margin="0,0,0,15"
                                 materialDesign:HintAssist.Hint="Optional - Service provider/vendor"/>

                        <!-- Spent By -->
                        <TextBlock Text="Spent By" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding AllUsers}" 
                                  SelectedItem="{Binding SelectedSpentByUser}"
                                  DisplayMemberPath="Username"
                                  Style="{StaticResource MaterialDesignComboBox}" Margin="0,0,0,15"
                                  materialDesign:HintAssist.Hint="Who spent this money?"/>

                        <!-- Payment Mode -->
                        <TextBlock Text="Payment Mode *" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding PaymentModes}" 
                                  SelectedItem="{Binding SelectedPaymentMode}"
                                  Style="{StaticResource MaterialDesignComboBox}" Margin="0,0,0,15"/>

                        <!-- Notes -->
                        <TextBlock Text="Notes" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource MaterialDesignTextBox}"
                                 TextWrapping="Wrap" AcceptsReturn="True" 
                                 Height="60" Margin="0,0,0,15"/>

                        <!-- Buttons -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Command="{Binding SaveExpenseCommand}" Width="120" Margin="0,0,10,0">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                        <Setter Property="Content" Value="Add Expense"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                                <Setter Property="Content" Value="Update"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="Clear" Command="{Binding ClearFormCommand}"
                                    Style="{StaticResource Button.Secondary}" Width="80"/>
                        </StackPanel>

                        <!-- Error Message -->
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="#f44336" TextWrapping="Wrap" 
                                   Margin="0,10,0,0" Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- RIGHT: Expenses List -->
            <Border Grid.Column="2" BorderBrush="#444" BorderThickness="1" CornerRadius="8" Background="#2a2a2a" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Filters -->
                    <Expander Grid.Row="0" Header="Filters" Foreground="White" Margin="0,0,0,10">
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="From Date" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,0,0,5"/>
                                <DatePicker SelectedDate="{Binding FilterStartDate}" Style="{StaticResource MaterialDesignDatePicker}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="To Date" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,0,0,5"/>
                                <DatePicker SelectedDate="{Binding FilterEndDate}" Style="{StaticResource MaterialDesignDatePicker}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Category" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,0,0,5"/>
                                <ComboBox ItemsSource="{Binding ActiveCategories}" 
                                          SelectedItem="{Binding FilterCategory}"
                                          DisplayMemberPath="CategoryName"
                                          Style="{StaticResource MaterialDesignComboBox}"/>
                            </StackPanel>

                            <Button Grid.Column="6" Content="Clear" Command="{Binding ClearFiltersCommand}"
                                    Style="{StaticResource Button.Secondary}" Width="80" VerticalAlignment="Bottom"/>
                        </Grid>
                    </Expander>

                    <!-- Total Count -->
                    <TextBlock Grid.Row="1" Text="{Binding ExpenseCount, StringFormat='{}{0} expenses'}"
                               Foreground="{StaticResource Text.Secondary}" FontSize="12" Margin="0,0,0,10"/>

                    <!-- Expenses List -->
                    <DataGrid Grid.Row="2" ItemsSource="{Binding PaginatedExpenses}"
                              SelectedItem="{Binding SelectedExpense}"
                              Style="{StaticResource MaterialDesignDataGrid}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserAddRows="False" CanUserDeleteRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding ExpenseDate, StringFormat='{}{0:dd-MMM-yyyy}'}" Width="100"/>
                            <DataGridTextColumn Header="Category" Binding="{Binding CategoryDisplay}" Width="*" MinWidth="120"/>
                            <DataGridTextColumn Header="Vendor" Binding="{Binding VendorDisplay}" Width="*" MinWidth="100"/>
                            <DataGridTextColumn Header="Spent By" Binding="{Binding SpentByDisplay}" Width="100"/>
                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat='₹{0:N2}'}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextAlignment" Value="Right"/>
                                        <Setter Property="Foreground" Value="#f44336"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Mode" Binding="{Binding PaymentMode}" Width="70"/>
                            <DataGridTemplateColumn Header="Actions" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource DataGrid.EditButton}"
                                                    Command="{Binding DataContext.EditExpenseCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Edit">
                                                <materialDesign:PackIcon Kind="Pencil" Width="16" Height="16"/>
                                            </Button>
                                            <Button Style="{StaticResource DataGrid.EditButton}"
                                                    Command="{Binding DataContext.DeleteExpenseCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Delete"
                                                    Margin="4,0,0,0"
                                                    Foreground="{StaticResource ErrorColor}">
                                                <materialDesign:PackIcon Kind="Delete" Width="16" Height="16"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Pagination -->
                    <controls:PaginationControl Grid.Row="3" DataContext="{Binding}" Margin="0,15,0,0"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="100" Height="100" Style="{StaticResource MaterialDesignCircularProgressBar}"/>
                <TextBlock Text="Loading..." Foreground="White" FontSize="16" Margin="0,20,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
