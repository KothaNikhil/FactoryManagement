<UserControl x:Class="FactoryManagement.Views.ExpenseCategoryManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:FactoryManagement.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800"
        d:DesignWidth="1200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Form Section -->
            <Border Grid.Column="0"
                    Style="{StaticResource FormCard}">
                <StackPanel>
                    <!-- Form Title -->
                    <controls:SectionHeader Margin="0,0,0,20">
                        <controls:SectionHeader.Style>
                            <Style TargetType="controls:SectionHeader">
                                <Setter Property="Title"
                                        Value="Add Category"/>
                                <Setter Property="Subtitle"
                                        Value="Create a new expense category"/>
                                <Setter Property="Icon"
                                        Value="➕"/>
                                <Setter Property="IconBackground"
                                        Value="{StaticResource Card.Success}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEditMode}"
                                            Value="True">
                                        <Setter Property="Title"
                                                Value="Edit Category"/>
                                        <Setter Property="Subtitle"
                                                Value="Update category details"/>
                                        <Setter Property="Icon"
                                                Value="✏️"/>
                                        <Setter Property="IconBackground"
                                                Value="{StaticResource Card.Warning}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:SectionHeader.Style>
                    </controls:SectionHeader>

                    <!-- Category Name -->
                    <TextBlock Text="Category Name *"
                            Foreground="{StaticResource Text.Secondary}"
                            FontSize="11"
                            Margin="0,0,0,5"/>
                    <TextBox Text="{Binding CategoryName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource TextBox.Standard}"
                            Margin="0,0,0,15"/>

                    <!-- Description -->
                    <TextBlock Text="Description"
                            Foreground="{StaticResource Text.Secondary}"
                            FontSize="11"
                            Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource TextBox.Standard}"
                             TextWrapping="Wrap"
                            AcceptsReturn="True"
                             Height="60"
                            Margin="0,0,0,15"/>

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                        <Button Command="{Binding SaveCategoryCommand}"
                                Width="120"
                                Margin="0,0,10,0">
                            <Button.Style>
                                <Style TargetType="Button"
                                        BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Setter Property="Content"
                                            Value="Add Category"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditMode}"
                                                Value="True">
                                            <Setter Property="Content"
                                                    Value="Update"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="Clear"
                                Command="{Binding ClearFormCommand}"
                                Style="{StaticResource Button.Secondary}"
                                Width="80"/>
                    </StackPanel>

                    <!-- Error Message -->
                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="{StaticResource ErrorColor}"
                               TextWrapping="Wrap"
                               Margin="0,10,0,0"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                </StackPanel>
            </Border>

            <!-- Categories List Section -->
            <Border Grid.Column="2"
                    Style="{StaticResource FormCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header with Search -->
                    <Border Grid.Row="0" Background="{StaticResource SecondaryBackground}" Padding="15" BorderBrush="{StaticResource BorderPrimary}" BorderThickness="0,0,0,1" Margin="-24,-24,-24,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0" VerticalAlignment="Center">
                                <Border Background="{StaticResource Card.Info}" Style="{StaticResource IconBadge}" Margin="0,0,12,0">
                                    <TextBlock Text="📋" FontSize="20" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Expense Categories" Style="{StaticResource SectionHeader.Title}"/>
                                </StackPanel>
                            </StackPanel>

                            <controls:SearchBox Grid.Column="2" 
                                              PlaceholderText="Search categories..." 
                                              Width="300"
                                              Margin="0"
                                              VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Categories List -->
                    <DataGrid Grid.Row="1"
                            ItemsSource="{Binding PaginatedCategories}"
                              SelectedItem="{Binding SelectedCategory}"
                              Style="{StaticResource DataGrid.Standard}"
                              ColumnHeaderStyle="{StaticResource DataGrid.Header.Standard}"
                              Margin="0,10,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Category"
                                    Binding="{Binding CategoryName}"
                                    Width="*"
                                    MinWidth="150"/>
                            <DataGridTextColumn Header="Description"
                                    Binding="{Binding Description}"
                                    Width="2*"
                                    MinWidth="200"/>
                            <DataGridTemplateColumn Header="Actions"
                                    Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource DataGrid.EditButton}"
                                                    Command="{Binding DataContext.EditCategoryCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Edit">
                                                <materialDesign:PackIcon Kind="Pencil"
                                                        Width="16"
                                                        Height="16"/>
                                            </Button>
                                            <Button Style="{StaticResource DataGrid.EditButton}"
                                                    Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Delete"
                                                    Margin="4,0,0,0"
                                                    Foreground="{StaticResource ErrorColor}">
                                                <materialDesign:PackIcon Kind="Delete"
                                                        Width="16"
                                                        Height="16"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Pagination -->
                    <controls:PaginationControl Grid.Row="2"
                            DataContext="{Binding}"
                            Margin="0,15,0,0"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="2"
                Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                        Width="100"
                        Height="100"
                        Style="{StaticResource MaterialDesignCircularProgressBar}"/>
                <TextBlock Text="Loading..."
                        Foreground="White"
                        FontSize="16"
                        Margin="0,20,0,0"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
