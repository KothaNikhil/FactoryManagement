<UserControl x:Class="FactoryManagement.Views.OperationalExpensesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:FactoryManagement.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800"
        d:DesignWidth="1200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Summary Cards -->
        <Grid Grid.Row="0"
                Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Expenses Card -->
            <controls:SummaryCard Grid.Column="0"
                                  Label="Total Expenses"
                                  Value="{Binding TotalExpenses, StringFormat='₹{0:N2}'}"
                                  Icon="💸"
                                  CardBackground="{StaticResource Card.Danger}"
                                  LabelForeground="{StaticResource Card.Danger.Label}"/>

            <!-- Monthly Expenses Card -->
            <controls:SummaryCard Grid.Column="2"
                                  Label="This Month"
                                  Value="{Binding MonthlyExpenses, StringFormat='₹{0:N2}'}"
                                  Icon="📅"
                                  CardBackground="{StaticResource Card.Warning}"
                                  LabelForeground="{StaticResource Card.Warning.Label}"/>

            <!-- Today's Expenses Card -->
            <controls:SummaryCard Grid.Column="4"
                                  Label="Today"
                                  Value="{Binding TodayExpenses, StringFormat='₹{0:N2}'}"
                                  Icon="🕒"
                                  CardBackground="{StaticResource Card.Info}"
                                  LabelForeground="{StaticResource Card.Info.Label}"/>

            <!-- Records Card -->
            <controls:SummaryCard Grid.Column="6"
                                  Label="Records"
                                  Value="{Binding ExpenseCount}"
                                  Icon="📊"
                                  CardBackground="{StaticResource Card.Success}"
                                  LabelForeground="{StaticResource Card.Success.Label}"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Form Section -->
            <Border Grid.Column="0"
                    Style="{StaticResource FormCard}">
                <StackPanel>
                    <!-- Form Title -->
                    <controls:SectionHeader Margin="0,0,0,20">
                        <controls:SectionHeader.Style>
                            <Style TargetType="controls:SectionHeader">
                                <Setter Property="Title"
                                        Value="Add Expense"/>
                                <Setter Property="Subtitle"
                                        Value="Record a new expense"/>
                                <Setter Property="Icon"
                                        Value="➕"/>
                                <Setter Property="IconBackground"
                                        Value="{StaticResource Card.Success}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEditMode}"
                                            Value="True">
                                        <Setter Property="Title"
                                                Value="Edit Expense"/>
                                        <Setter Property="Subtitle"
                                                Value="Update expense details"/>
                                        <Setter Property="Icon"
                                                Value="✏️"/>
                                        <Setter Property="IconBackground"
                                                Value="{StaticResource Card.Warning}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:SectionHeader.Style>
                    </controls:SectionHeader>

                    <!-- Category -->
                    <TextBlock Text="Category *"
                            Foreground="{StaticResource Text.Secondary}"
                            FontSize="11"
                            Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding ActiveCategories}"
                              SelectedItem="{Binding SelectedCategory}"
                              DisplayMemberPath="CategoryName"
                              Style="{StaticResource ComboBox.Standard}"
                              Margin="0,0,0,15"/>

                    <!-- Amount -->
                    <TextBlock Text="Amount *"
                            Foreground="{StaticResource Text.Secondary}"
                            FontSize="11"
                            Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource TextBox.Standard}"
                             Margin="0,0,0,15"/>

                    <!-- Expense Date -->
                    <TextBlock Text="Expense Date *"
                            Foreground="{StaticResource Text.Secondary}"
                            FontSize="11"
                            Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding ExpenseDate}"
                                Style="{StaticResource DatePicker.Standard}"
                                Margin="0,0,0,15"/>

                    <!-- Spent By -->
                    <TextBlock Text="Spent By"
                            Foreground="{StaticResource Text.Secondary}"
                            FontSize="11"
                            Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding AllUsers}"
                              SelectedItem="{Binding SelectedSpentByUser}"
                              DisplayMemberPath="Username"
                              Style="{StaticResource ComboBox.Standard}"
                              materialDesign:HintAssist.Hint="Who spent this money?"
                              Margin="0,0,0,15"/>

                    <!-- Payment Mode -->
                    <TextBlock Text="Payment Mode *"
                            Foreground="{StaticResource Text.Secondary}"
                            FontSize="11"
                            Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding PaymentModes}"
                              SelectedItem="{Binding SelectedPaymentMode}"
                              Style="{StaticResource ComboBox.Standard}"
                              Margin="0,0,0,15"/>

                    <!-- Notes -->
                    <TextBlock Text="Notes"
                            Foreground="{StaticResource Text.Secondary}"
                            FontSize="11"
                            Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource TextBox.Standard}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             Height="60"
                             Margin="0,0,0,15"/>

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                        <Button Command="{Binding SaveExpenseCommand}"
                                Width="120"
                                Margin="0,0,10,0">
                            <Button.Style>
                                <Style TargetType="Button"
                                        BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Setter Property="Content"
                                            Value="Add Expense"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditMode}"
                                                Value="True">
                                            <Setter Property="Content"
                                                    Value="Update"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="Clear"
                                Command="{Binding ClearFormCommand}"
                                Style="{StaticResource Button.Secondary}"
                                Width="80"/>
                    </StackPanel>

                    <!-- Error Message -->
                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="{StaticResource ErrorColor}"
                               TextWrapping="Wrap"
                               Margin="0,10,0,0"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                </StackPanel>
            </Border>

            <!-- Expenses List Section -->
            <Border Grid.Column="2"
                    Style="{StaticResource FormCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header with Filters -->
                    <Border Grid.Row="0" Background="{StaticResource SecondaryBackground}" Padding="15" BorderBrush="{StaticResource BorderPrimary}" BorderThickness="0,0,0,1" Margin="-24,-24,-24,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0" VerticalAlignment="Center">
                                <Border Background="{StaticResource Card.Info}" Style="{StaticResource IconBadge}" Margin="0,0,12,0">
                                    <TextBlock Text="📋" FontSize="20" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Expense Records" Style="{StaticResource SectionHeader.Title}"/>
                                </StackPanel>
                            </StackPanel>

                            <DatePicker Grid.Column="4" 
                                        SelectedDate="{Binding FilterStartDate}"
                                        Style="{StaticResource DatePicker.Standard}"
                                        materialDesign:HintAssist.Hint="From Date"
                                        VerticalAlignment="Center"/>

                            <DatePicker Grid.Column="6" 
                                        SelectedDate="{Binding FilterEndDate}"
                                        Style="{StaticResource DatePicker.Standard}"
                                        materialDesign:HintAssist.Hint="To Date"
                                        VerticalAlignment="Center"/>

                            <ComboBox Grid.Column="8" 
                                      ItemsSource="{Binding ActiveCategories}"
                                      SelectedItem="{Binding FilterCategory}"
                                      DisplayMemberPath="CategoryName"
                                      Style="{StaticResource ComboBox.Standard}"
                                      materialDesign:HintAssist.Hint="Category"
                                      VerticalAlignment="Center"/>

                            <Button Grid.Column="10" 
                                    Content="Clear"
                                    Command="{Binding ClearFiltersCommand}"
                                    Style="{StaticResource Button.Secondary}"
                                    Width="80"
                                    VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Expenses List -->
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding PaginatedExpenses}"
                              SelectedItem="{Binding SelectedExpense}"
                              Style="{StaticResource DataGrid.Standard}"
                              ColumnHeaderStyle="{StaticResource DataGrid.Header.Standard}"
                              Margin="0,10,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date"
                                    Binding="{Binding ExpenseDate, StringFormat='dd-MMM-yyyy'}"
                                    Width="100"/>
                            <DataGridTextColumn Header="Category"
                                    Binding="{Binding CategoryDisplay}"
                                    Width="*"
                                    MinWidth="120"/>
                            <DataGridTextColumn Header="Spent By"
                                    Binding="{Binding SpentByDisplay}"
                                    Width="100"/>
                            <DataGridTextColumn Header="Amount"
                                    Binding="{Binding Amount, StringFormat='₹{0:N2}'}"
                                    Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextAlignment"
                                                Value="Right"/>
                                        <Setter Property="FontWeight"
                                                Value="SemiBold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Mode"
                                    Binding="{Binding PaymentMode}"
                                    Width="80"/>
                            <DataGridTemplateColumn Header="Actions"
                                    Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource DataGrid.EditButton}"
                                                    Command="{Binding DataContext.EditExpenseCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Edit">
                                                <materialDesign:PackIcon Kind="Pencil"
                                                        Width="16"
                                                        Height="16"/>
                                            </Button>
                                            <Button Style="{StaticResource DataGrid.EditButton}"
                                                    Command="{Binding DataContext.DeleteExpenseCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Delete"
                                                    Margin="4,0,0,0"
                                                    Foreground="{StaticResource ErrorColor}">
                                                <materialDesign:PackIcon Kind="Delete"
                                                        Width="16"
                                                        Height="16"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Pagination -->
                    <controls:PaginationControl Grid.Row="2"
                            DataContext="{Binding}"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="2"
                Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                             Width="100"
                             Height="100"
                             Style="{StaticResource MaterialDesignCircularProgressBar}"/>
                <TextBlock Text="Loading..."
                           Foreground="White"
                           FontSize="16"
                           Margin="0,20,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
