<UserControl x:Class="FactoryManagement.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:FactoryManagement.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Summary Cards (Top Row) - Simplified to 3 cards -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Transactions Today Card -->
                <controls:SummaryCard Grid.Column="0"
                                      Label="Transactions Today"
                                      Value="{Binding TransactionCount}"
                                      Icon="📊"
                                      CardBackground="{StaticResource Card.Info}"
                                      LabelForeground="{StaticResource Card.Info.Label}"/>

                <!-- Total Amount Transacted Card -->
                <controls:SummaryCard Grid.Column="2"
                                      Label="Total Amount"
                                      Value="{Binding TotalAmountTransacted, StringFormat='₹{0:N2}'}"
                                      Icon="💵"
                                      CardBackground="{StaticResource Card.Success}"
                                      LabelForeground="{StaticResource Card.Success.Label}"/>

                <!-- Cash in Hand Card -->
                <controls:SummaryCard Grid.Column="4"
                                      Label="Cash in Hand"
                                      Value="{Binding CurrentCashInHand, StringFormat='₹{0:N2}'}"
                                      Icon="💰"
                                      CardBackground="{StaticResource Card.Warning}"
                                      LabelForeground="{StaticResource Card.Warning.Label}"/>
            </Grid>

            <!-- Row 1 removed - simplified -->

            <!-- Main Content Area -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="1.2*"/>
                </Grid.ColumnDefinitions>

                <!-- Recent Entries Table -->
                <Border Grid.Column="0" Style="{StaticResource FormCard}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <controls:SectionHeader Grid.Row="0" 
                                              Title="Recent Entries" 
                                              Subtitle="Latest 15 transactions across all categories" 
                                              Icon="📊" 
                                              IconBackground="{StaticResource Card.Info}" />

                        <!-- DataGrid -->
                        <DataGrid Grid.Row="1" ItemsSource="{Binding PaginatedAllTransactions}" Style="{StaticResource Section.DataGrid}" KeyboardNavigation.TabNavigation="Once">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGrid.Header.Standard}"/>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                                <DataGridTextColumn Header="Date" Binding="{Binding TransactionDate, StringFormat='dd-MMM-yyyy HH:mm'}" Width="140"/>
                                <DataGridTextColumn Header="Type" Binding="{Binding TransactionType, Converter={StaticResource EnumToFriendlyStringConverter}}" Width="120"/>
                                <DataGridTextColumn Header="Item" Binding="{Binding Item}" Width="*"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Details" Binding="{Binding AdditionalInfo}" Width="*"/>
                                <DataGridTextColumn Header="Entered By" Binding="{Binding EnteredBy}" Width="100"/>
                                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat='₹{0:N2}'}" Width="130">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    
                    <!-- Pagination Control -->
                    <controls:PaginationControl Grid.Row="2"
                        CurrentPage="{Binding CurrentPage, Mode=TwoWay}"
                        TotalPages="{Binding TotalPages}"
                        TotalRecords="{Binding TotalRecords}"
                        CanGoToPreviousPage="{Binding CanGoToPreviousPage}"
                        CanGoToNextPage="{Binding CanGoToNextPage}"
                        FirstPageCommand="{Binding GoToFirstPageCommand}"
                        PreviousPageCommand="{Binding GoToPreviousPageCommand}"
                        NextPageCommand="{Binding GoToNextPageCommand}"
                        LastPageCommand="{Binding GoToLastPageCommand}"
                        Margin="0,16,0,0"/>
                    </Grid>
                </Border>

                <!-- Stock Levels Panel -->
                <Border Grid.Column="2" Style="{StaticResource FormCard}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <controls:SectionHeader Grid.Row="0" 
                                              Title="Stock Levels" 
                                              Subtitle="Current Inventory Status" 
                                              Icon="📦" 
                                              IconBackground="{StaticResource Card.Info}" />

                        <!-- Stock Chart -->
                        <ScrollViewer Grid.Row="1" Style="{StaticResource Section.ScrollViewer}">
                            <ItemsControl ItemsSource="{Binding AllItems}" Style="{StaticResource Section.ItemsControl}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="0,8" BorderBrush="{StaticResource Card.Border}" BorderThickness="0,0,0,1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="60"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="60"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <!-- Item Name -->
                                                <TextBlock Grid.Column="0"
                                                         Text="{Binding ItemName}" 
                                                         FontWeight="SemiBold"
                                                         FontSize="11"
                                                         Foreground="{StaticResource Text.Primary}"
                                                         VerticalAlignment="Center"
                                                         TextTrimming="CharacterEllipsis"
                                                         ToolTip="{Binding ItemName}"/>
                                                
                                                <!-- Horizontal Bar Chart (0-10000 scale) -->
                                                <Grid Grid.Column="1" Margin="12,0,12,0" VerticalAlignment="Center">
                                                    <!-- Background bar (0-10000) -->
                                                    <Border Background="{StaticResource Card.Header}" 
                                                           CornerRadius="3"
                                                           Height="20"/>
                                                    <!-- Filled bar (based on percentage) -->
                                                    <Border HorizontalAlignment="Left" 
                                                           CornerRadius="3"
                                                           Height="20">
                                                        <Border.Width>
                                                            <MultiBinding Converter="{StaticResource StockBarWidthConverter}" ConverterParameter="10000">
                                                                <Binding Path="CurrentStock"/>
                                                                <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Grid}"/>
                                                            </MultiBinding>
                                                        </Border.Width>
                                                        <Border.Background>
                                                            <Binding Path="CurrentStock" Converter="{StaticResource StockLevelPercentConverter}" ConverterParameter="10000"/>
                                                        </Border.Background>
                                                    </Border>
                                                </Grid>
                                                
                                                <!-- Stock Value and Unit -->
                                                <StackPanel Grid.Column="2"
                                                         HorizontalAlignment="Right"
                                                         VerticalAlignment="Center">
                                                    <TextBlock FontWeight="Bold"
                                                             FontSize="11"
                                                             Foreground="{StaticResource TextPrimary}">
                                                        <Run Text="{Binding CurrentStock, StringFormat=N0, Mode=OneWay}"/>
                                                        <Run Text=" "/>
                                                        <Run Text="{Binding Unit, Mode=OneWay}" 
                                                             FontSize="9"
                                                             Foreground="{StaticResource Text.Secondary}"/>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- Loading Overlay -->
            <Border Grid.RowSpan="3" Background="{StaticResource OverlayDark}"
                    Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="250" Height="6" Foreground="{StaticResource ButtonPrimary}"/>
                    <TextBlock Text="Loading..." Foreground="White" FontSize="18" Margin="0,15,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
